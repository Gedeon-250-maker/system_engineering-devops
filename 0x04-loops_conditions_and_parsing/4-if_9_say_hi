#!/usr/bin/env bash
# This script displays "Best School" 10 times, with "Hi" on the 9th iteration.

COUNT=0
while [ "$COUNT" -lt 10 ]; do
  echo "Best School"
  if [ "$COUNT" -eq 8 ]; then # Check if this is the 9th iteration (0-indexed 8)
    echo "Hi"
  fi
  ((COUNT++))
done
